server:
  port: 8080
  error:
    whitelabel:
      enabled: false

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}
  rate-limit:
    random-address-per-minute: ${APP_RATE_LIMIT_RANDOM_ADDRESS_PER_MINUTE:1000}
